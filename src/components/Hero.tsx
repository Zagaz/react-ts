import React from 'react'
import {useState} from 'react'
interface HeroProps {
     title: string,
     subtitle: string
}

const Hero:React.FC<HeroProps> = ({title, subtitle}) => {


     return (
          <>
    
           </>
     )
}

export default Hero